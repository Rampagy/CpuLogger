#if !defined(TEMPERATURE_HPP)
#define TEMPERATURE_HPP

#include <map>
#include <string>
#include <filesystem>
//#include <iostream>
#include <fstream>

void GetTemperatures( std::map<std::string, float>* temperatureInfo );

#endif